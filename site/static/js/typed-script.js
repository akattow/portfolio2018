$(function () {
  $(".element").typed({
    strings: ["user experience designer", "front-end developer", "pug enthusiast"],
    typeSpeed: 0,
    backDelay: 800,
    loop: true,
    loopCount: null,
  });
});
